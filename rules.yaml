features:
  - name: api
    scopes:
      - match: module
        prefix: "{{.Module.Name|snake}}/"
        documents:
          - source: "api/index.ts.tpl"
            target: "api/index.ts"
  - name: scaffold
    scopes:
      - match: interface
        prefix: "{{.Module.Name|snake}}/"
        documents:
          - source: "scaffold/interface.ts.tpl"
            target: "impl/{{Camel .Interface.Name}}.ts"
